# GraphQL (with apollo server) template

## installation

```cmd
# get package.json & set type="module"
npm init --yes && npm pkg set type="module"
# init git version control
git init
# install dependencies
npm install @apollo/server graphql
```
